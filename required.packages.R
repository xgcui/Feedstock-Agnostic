# Type: R script

# AUTHOR:
# XG CUI, 2017 October
# JBEI Sustainability Research Group
# Project: Feedstock Agnostic Biorefinery Study

# PURPOSE:
# With script, it can test which required packages are not installed.
####################################################################

require(ggplot2)
require(ggmap)
require(raster)
require(sp)
require(rgeos)
require(spatialEco)
require(geosphere)
require(igraph)
require(shp2graph)
require(rgdal)
require(kimisc)
require(knitr)
require(R.utils)
require(spatial)
require(GISTools)
require(gdata)
require(maptools)
require(plyr)
require(dplyr)
require(doParallel)
require(iterators)
require(foreach)
require(doSNOW)
require(snow)
require(broom)
require(colorspace)
require(parallel)
require(rmapshaper)
require(spdep)
require(cluster)
require(fpc)
require(RColorBrewer)
require(openxlsx)
require(stats)
require(Hmisc)
require(tidyr)


